#include "holberton.h"

/**
 *_strchr - function that locates a character in a string
 * @s: buffer
 * @c: char
 * Return: s
 **/
char *_strchr(char *s, char c)
{
	int i;

	for (i = 0; s[i] != 'c'; i++)
	{
		if s[i] = 'c'
		    break;
	}
		return (s);
}
